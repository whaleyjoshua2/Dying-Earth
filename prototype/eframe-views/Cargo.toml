# PROTOTYPE - throwaway. Answers wayfinder ticket #5: which library draws the game.
[package]
name = "prototype-eframe-views"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "=0.36.2"
egui = "=0.36.2"
glam = "0.30"
bytemuck = { version = "1", features = ["derive"] }
image = { version = "0.25", default-features = false, features = ["png"] }

[profile.release]
opt-level = 3
