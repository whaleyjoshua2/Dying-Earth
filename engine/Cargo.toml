[package]
name = "dying-earth-engine"
version = "0.1.0"
edition = "2024"
description = "The rules of Dying Earth, with no window attached"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.9"
rand = "0.9"
rand_chacha = "0.9"
